doctype html 
html(lang='en')
  include ../common/head.pug
  +head("Starter MVP")
  body 
        form(action="/lead/" + lead.id + "/edit/", method="POST")
            input(name="lead_email", type="email", class="form-control" placeholder="Enter your email", value=lead.email)
            |
            |
            button(type="submit").btn.btn-primary    Save